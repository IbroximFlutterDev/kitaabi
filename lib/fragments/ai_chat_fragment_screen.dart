import 'package:flutter/material.dart';

class AIChatFragmentScreen extends StatelessWidget {
  const AIChatFragmentScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Image.asset('images/ai.png'),
      ),
    );
  }
}
